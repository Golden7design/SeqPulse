global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "seqpulse-backend"
    metrics_path: /metrics
    static_configs:
      # Prometheus runs with `network_mode: host` in docker-compose.
      # This works even if backend is bound to 127.0.0.1:8000.
      - targets: ["localhost:8000"]
